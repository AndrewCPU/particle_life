typedef struct

__kernel void sampleKernel(__global const float *a, __global const float *b, __global float *c)
{
    int gid = get_global_id(0);
    c[gid] = sqrt((a[gid] * a[gid]) + (b[gid]*b[gid]));
}
